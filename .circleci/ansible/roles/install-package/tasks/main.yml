---
- name: upgrade all packages
  become: true
  apt:
    upgrade: "yes"

# - name: Install python
#   become_user: ubuntu
#   shell: |
#     sudo apt-get update
#     sudo apt-get install -y python3 python3-pip

- name: "install dependencies."
  become: true
  apt:
    name: ["nodejs", "npm"]
    state: latest
    update_cache: yes

- name: "install pm2"
  become: true
  npm:
    name: pm2
    global: yes
    production: yes
    state: present
